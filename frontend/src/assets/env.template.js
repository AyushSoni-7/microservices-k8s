(function(window) {
  window["env"] = window["env"] || {};

  // Environment variables
  window["env"]["category_service_url"] = "${category_service_url}";
  window["env"]["product_service_url"] = "${product_service_url}";
})(this);
